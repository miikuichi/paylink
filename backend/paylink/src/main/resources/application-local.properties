# Local-only overrides — DO NOT COMMIT REAL SECRETS (this file is gitignored).
# Fill in your actual Supabase connection details below.

# --- Supabase Postgres connection ---
spring.datasource.url=jdbc:postgresql://<PROJECT_REF>.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=<YOUR_SUPABASE_DB_PASSWORD>
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JWT secret (use a long random string, at least 32 characters) ---
paylink.security.jwt.secret=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_AT_LEAST_32_CHARS
